diff --git a/app.py b/app.py
index e10fd49..7e94dbd 100644
--- a/app.py
+++ b/app.py
@@ -92,7 +92,7 @@ def init_db():
             """)
             c.execute("""
                 INSERT INTO dados (id, conteudo)
-                VALUES (1, '{"alunos": {}, "materias": {}, "usuarios": {}}')
+                VALUES (1, '{"alunos": {}, "materias": {}, "usuarios": {}, "turmas": {}}')
                 ON CONFLICT (id) DO NOTHING
             """)
         else:
@@ -104,7 +104,7 @@ def init_db():
             """)
             c.execute("""
                 INSERT OR IGNORE INTO dados (id, conteudo)
-                VALUES (1, '{"alunos": {}, "materias": {}, "usuarios": {}}')
+                VALUES (1, '{"alunos": {}, "materias": {}, "usuarios": {}, "turmas": {}}')
             """)
         conn.commit()
     finally:
@@ -127,6 +127,8 @@ def load():
             d = val
         if "usuarios" not in d:
             d["usuarios"] = {}
+        if "turmas" not in d:
+            d["turmas"] = {}
         return d
     finally:
         c.close()
@@ -207,14 +209,17 @@ def add_aluno():
     d = load()
     body = request.json
     nome = sanitize(body.get("nome", ""))
+    turma = sanitize(body.get("turma", "")) if body.get("turma") else ""
     if not nome:
         return jsonify({"erro": "Nome é obrigatório."}), 400
     if any(a["nome"].lower() == nome.lower() for a in d["alunos"].values()):
         return jsonify({"erro": "Aluno já cadastrado."}), 400
+    if turma and turma not in d["turmas"]:
+        return jsonify({"erro": "Turma não encontrada."}), 404
     mat = str(uuid.uuid4())[:8]
     while mat in d["alunos"]:
         mat = str(uuid.uuid4())[:8]
-    d["alunos"][mat] = {"nome": nome, "materias": []}
+    d["alunos"][mat] = {"nome": nome, "materias": [], "turma": turma}
     save(d)
     return jsonify({"ok": True})
 
@@ -239,12 +244,18 @@ def edit_aluno(matricula):
     matricula = sanitize(matricula)
     if matricula not in d["alunos"]:
         return jsonify({"erro": "Aluno não encontrado."}), 404
-    nome = sanitize(request.json.get("nome", ""))
+    body = request.json
+    nome = sanitize(body.get("nome", ""))
     if not nome:
         return jsonify({"erro": "Nome é obrigatório."}), 400
     if any(a["nome"].lower() == nome.lower() and k != matricula for k, a in d["alunos"].items()):
         return jsonify({"erro": "Já existe um aluno com esse nome."}), 400
     d["alunos"][matricula]["nome"] = nome
+    if "turma" in body:
+        turma = sanitize(body.get("turma", "")) if body.get("turma") else ""
+        if turma and turma not in d["turmas"]:
+            return jsonify({"erro": "Turma não encontrada."}), 404
+        d["alunos"][matricula]["turma"] = turma
     save(d)
     return jsonify({"ok": True})
 
@@ -303,6 +314,58 @@ def del_materia(nome):
     save(d)
     return jsonify({"ok": True})
 
+@app.route("/api/turmas", methods=["POST"])
+@login_required
+def add_turma():
+    d = load()
+    nome = sanitize(request.json.get("nome", ""))
+    if not nome:
+        return jsonify({"erro": "Nome da turma é obrigatório."}), 400
+    if nome in d["turmas"]:
+        return jsonify({"erro": "Turma já existe."}), 400
+    d["turmas"][nome] = {}
+    save(d)
+    return jsonify({"ok": True})
+
+@app.route("/api/turmas/<nome>", methods=["DELETE"])
+@login_required
+def del_turma(nome):
+    d = load()
+    nome = sanitize(nome)
+    if nome not in d["turmas"]:
+        return jsonify({"erro": "Turma não encontrada."}), 404
+    del d["turmas"][nome]
+    for aluno in d["alunos"].values():
+        if aluno.get("turma") == nome:
+            aluno["turma"] = ""
+    save(d)
+    return jsonify({"ok": True})
+
+@app.route("/api/alunos/<matricula>/turma", methods=["POST"])
+@login_required
+def assoc_turma(matricula):
+    d = load()
+    matricula = sanitize(matricula)
+    turma = sanitize(request.json.get("turma", ""))
+    if matricula not in d["alunos"]:
+        return jsonify({"erro": "Aluno não encontrado."}), 404
+    if turma not in d["turmas"]:
+        return jsonify({"erro": "Turma não encontrada."}), 404
+    d["alunos"][matricula]["turma"] = turma
+    save(d)
+    return jsonify({"ok": True})
+
+@app.route("/api/alunos/<matricula>/turma", methods=["DELETE"])
+@login_required
+def desassoc_turma(matricula):
+    d = load()
+    matricula = sanitize(matricula)
+    if matricula not in d["alunos"]:
+        return jsonify({"erro": "Aluno não encontrado."}), 404
+    d["alunos"][matricula]["turma"] = ""
+    save(d)
+    return jsonify({"ok": True})
+
 @app.route("/api/chamada", methods=["POST"])
 @login_required
 def salvar_chamada():
diff --git a/templates/index.html b/templates/index.html
index e82e6ca..57ff3a3 100644
--- a/templates/index.html
+++ b/templates/index.html
@@ -423,6 +423,7 @@ tr:last-child td {
 <div class="tabs-container">
   <div class="tabs">
     <button class="tab active" onclick="showTab('alunos',this)">Alunos</button>
+    <button class="tab" onclick="showTab('turmas',this)">Turmas</button>
     <button class="tab" onclick="showTab('materias',this)">Matérias</button>
     <button class="tab" onclick="showTab('assoc',this)">Associar</button>
     <button class="tab" onclick="showTab('chamada',this)">Chamada</button>
@@ -441,23 +442,68 @@ tr:last-child td {
         <div class="form-group">
           <input id="a-nome" placeholder="Nome completo do aluno">
         </div>
+        <div class="form-group">
+          <select id="a-turma">
+            <option value="">Sem turma</option>
+          </select>
+        </div>
         <button class="btn btn-primary" onclick="addAluno()"><i class="ti ti-plus"></i> Cadastrar</button>
       </div>
       <div class="msg" id="a-msg"></div>
     </div>
     <div class="card">
       <h3>Alunos cadastrados</h3>
+      <div class="form-row" style="margin-bottom:12px">
+        <div class="form-group">
+          <select id="f-turma" onchange="renderAlunos()">
+            <option value="">Todas as turmas</option>
+          </select>
+        </div>
+      </div>
       <div class="table-container">
         <table id="t-alunos">
           <thead>
             <tr>
               <th>Nome</th>
+              <th>Turma</th>
               <th>Matérias</th>
               <th style="width: 100px;" class="text-center">Ações</th>
             </tr>
           </thead>
           <tbody>
-            <tr><td colspan="3" style="color:#94a3b8" class="text-center">Nenhum aluno cadastrado.</td></tr>
+            <tr><td colspan="4" style="color:#94a3b8" class="text-center">Nenhum aluno cadastrado.</td></tr>
+          </tbody>
+        </table>
+      </div>
+    </div>
+  </div>
+
+  <!-- SEÇÃO: TURMAS -->
+  <div id="sec-turmas" class="section">
+    <h2 class="section-title">Turmas</h2>
+    <div class="card">
+      <h3>Cadastrar turma</h3>
+      <div class="form-row">
+        <div class="form-group">
+          <input id="t-nome" placeholder="Nome da turma (ex: 9º Ano A)">
+        </div>
+        <button class="btn btn-primary" onclick="addTurma()"><i class="ti ti-plus"></i> Cadastrar</button>
+      </div>
+      <div class="msg" id="t-msg"></div>
+    </div>
+    <div class="card">
+      <h3>Turmas cadastradas</h3>
+      <div class="table-container">
+        <table id="t-turmas">
+          <thead>
+            <tr>
+              <th>Turma</th>
+              <th>Alunos</th>
+              <th style="width: 80px;" class="text-center">Ações</th>
+            </tr>
+          </thead>
+          <tbody>
+            <tr><td colspan="3" style="color:#94a3b8" class="text-center">Nenhuma turma cadastrada.</td></tr>
           </tbody>
         </table>
       </div>
@@ -502,6 +548,23 @@ tr:last-child td {
   <!-- SEÇÃO: ASSOCIAR -->
   <div id="sec-assoc" class="section">
     <h2 class="section-title">Associar</h2>
+    <div class="card">
+      <h3>Associar aluno a turma</h3>
+      <div class="form-row">
+        <div class="form-group">
+          <select id="att-aluno">
+            <option value="">Selecione o aluno</option>
+          </select>
+        </div>
+        <div class="form-group">
+          <select id="att-turma">
+            <option value="">Selecione a turma</option>
+          </select>
+        </div>
+        <button class="btn btn-primary" onclick="associarTurma()"><i class="ti ti-link"></i> Associar</button>
+      </div>
+      <div class="msg" id="att-msg"></div>
+    </div>
     <div class="card">
       <h3>Associar aluno a matéria</h3>
       <div class="form-row">
@@ -608,6 +671,9 @@ tr:last-child td {
   <div class="modal">
     <h3>Editar aluno</h3>
     <input id="edit-nome" placeholder="Novo nome">
+    <select id="edit-turma" style="margin-top:10px">
+      <option value="">Sem turma</option>
+    </select>
     <div class="msg" id="edit-msg"></div>
     <div class="modal-actions" style="margin-top:10px">
       <button class="btn" style="background:#f1f5f9; color:#475569" onclick="fecharModal()">Cancelar</button>
@@ -617,7 +683,7 @@ tr:last-child td {
 </div>
 
 <script>
-let dados={alunos:{},materias:{}};
+let dados={alunos:{},materias:{},turmas:{}};
 let editandoId=null;
 let currentEvaluations=[];
 
@@ -631,6 +697,7 @@ async function api(url,method='GET',body=null){
 async function refresh(){
   dados=await api('/api/dados');
   renderAlunos();
+  renderTurmas();
   renderMaterias();
   renderAssoc();
   populateSelects();
@@ -657,23 +724,46 @@ function alunosOrdenados(){
 
 function renderAlunos(){
   const tb=document.querySelector('#t-alunos tbody');
-  const rows=alunosOrdenados();
+  const filtro=document.getElementById('f-turma')?.value||'';
+  let rows=alunosOrdenados();
+  if(filtro) rows=rows.filter(([,a])=>(a.turma||'')===filtro);
   if(!rows.length){
-    tb.innerHTML='<tr><td colspan="3" style="color:#94a3b8" class="text-center">Nenhum aluno cadastrado.</td></tr>';
+    tb.innerHTML='<tr><td colspan="4" style="color:#94a3b8" class="text-center">Nenhum aluno cadastrado.</td></tr>';
     return;
   }
   tb.innerHTML=rows.map(([mat,a])=>`
     <tr>
       <td class="font-medium">${a.nome}</td>
+      <td>${a.turma?`<span class="badge">${a.turma}</span>`:'—'}</td>
       <td>${(a.materias||[]).map(m=>`<span class="badge">${m}</span>`).join('')||'—'}</td>
       <td class="text-center" style="display:flex; justify-content:center; gap:6px">
-        <button class="btn-icon btn-edit" onclick="abrirModal('${mat}','${a.nome.replace(/'/g,"\\'")}')"><i class="ti ti-pencil"></i></button>
+        <button class="btn-icon btn-edit" onclick="abrirModal('${mat}','${a.nome.replace(/'/g,"\\'")}','${(a.turma||'').replace(/'/g,"\\'")}')"><i class="ti ti-pencil"></i></button>
         <button class="btn-icon btn-delete" onclick="delAluno('${mat}')"><i class="ti ti-trash"></i></button>
       </td>
     </tr>
   `).join('');
 }
 
+function renderTurmas(){
+  const tb=document.querySelector('#t-turmas tbody');
+  const nomes=Object.keys(dados.turmas).sort((a,b)=>a.localeCompare(b,'pt-BR'));
+  if(!nomes.length){
+    tb.innerHTML='<tr><td colspan="3" style="color:#94a3b8" class="text-center">Nenhuma turma cadastrada.</td></tr>';
+    return;
+  }
+  tb.innerHTML=nomes.map(nome=>{
+    const qtd=alunosOrdenados().filter(([,a])=>a.turma===nome).length;
+    return `
+    <tr>
+      <td class="font-medium">${nome}</td>
+      <td>${qtd}</td>
+      <td class="text-center">
+        <button class="btn-icon btn-delete" onclick="delTurma('${nome.replace(/'/g,"\\'")}')"><i class="ti ti-trash"></i></button>
+      </td>
+    </tr>
+  `}).join('');
+}
+
 function renderMaterias(){
   const tb=document.querySelector('#t-materias tbody');
   const rows=Object.entries(dados.materias);
@@ -714,7 +804,8 @@ function renderAssoc(){
 function populateSelects(){
   const alunos=alunosOrdenados();
   const mats=Object.keys(dados.materias);
-  ['as-aluno','bl-aluno'].forEach(id=>{
+  const turmas=Object.keys(dados.turmas).sort((a,b)=>a.localeCompare(b,'pt-BR'));
+  ['as-aluno','bl-aluno','att-aluno'].forEach(id=>{
     const s=document.getElementById(id);
     const v=s.value;
     s.innerHTML='<option value="">Selecione o aluno</option>'+alunos.map(([m,a])=>`<option value="${m}">${a.nome}</option>`).join('');
@@ -726,11 +817,26 @@ function populateSelects(){
     s.innerHTML='<option value="">Selecione a matéria</option>'+mats.map(m=>`<option value="${m}">${m}</option>`).join('');
     s.value=v;
   });
+  const s=document.getElementById('a-turma');
+  const v1=s.value;
+  s.innerHTML='<option value="">Sem turma</option>'+turmas.map(t=>`<option value="${t}">${t}</option>`).join('');
+  s.value=v1;
+
+  const fs=document.getElementById('f-turma');
+  const v2=fs.value;
+  fs.innerHTML='<option value="">Todas as turmas</option>'+turmas.map(t=>`<option value="${t}">${t}</option>`).join('');
+  fs.value=v2;
+
+  const ts=document.getElementById('att-turma');
+  const v3=ts.value;
+  ts.innerHTML='<option value="">Selecione a turma</option>'+turmas.map(t=>`<option value="${t}">${t}</option>`).join('');
+  ts.value=v3;
 }
 
-function abrirModal(id,nome){
+function abrirModal(id,nome,turma){
   editandoId=id;
   document.getElementById('edit-nome').value=nome;
+  document.getElementById('edit-turma').value=turma||'';
   document.getElementById('edit-msg').textContent='';
   document.getElementById('modal-editar').style.display='flex';
 }
@@ -742,12 +848,13 @@ function fecharModal(){
 
 async function salvarEdicao(){
   const nome=document.getElementById('edit-nome').value.trim();
+  const turma=document.getElementById('edit-turma').value;
   if(!nome){
     document.getElementById('edit-msg').style.color='#ef4444';
     document.getElementById('edit-msg').textContent='Nome não pode ser vazio.';
     return;
   }
-  const r=await api('/api/alunos/'+encodeURIComponent(editandoId),'PUT',{nome});
+  const r=await api('/api/alunos/'+encodeURIComponent(editandoId),'PUT',{nome,turma});
   if(r.ok){
     fecharModal();
     refresh();
@@ -759,12 +866,14 @@ async function salvarEdicao(){
 
 async function addAluno(){
   const nome=document.getElementById('a-nome').value.trim();
-  const r=await api('/api/alunos','POST',{nome});
+  const turma=document.getElementById('a-turma').value;
+  const r=await api('/api/alunos','POST',{nome,turma});
   const msg=document.getElementById('a-msg');
   if(r.ok){
     msg.style.color='#10b981';
     msg.textContent='Aluno cadastrado com sucesso!';
     document.getElementById('a-nome').value='';
+    document.getElementById('a-turma').value='';
     refresh();
   }else{
     msg.style.color='#ef4444';
@@ -778,6 +887,47 @@ async function delAluno(mat){
   refresh();
 }
 
+async function addTurma(){
+  const nome=document.getElementById('t-nome').value.trim();
+  const r=await api('/api/turmas','POST',{nome});
+  const msg=document.getElementById('t-msg');
+  if(r.ok){
+    msg.style.color='#10b981';
+    msg.textContent='Turma cadastrada com sucesso!';
+    document.getElementById('t-nome').value='';
+    refresh();
+  }else{
+    msg.style.color='#ef4444';
+    msg.textContent=r.erro;
+  }
+}
+
+async function delTurma(nome){
+  if(!confirm('Excluir esta turma? Os alunos vinculados ficarão sem turma.'))return;
+  await api('/api/turmas/'+encodeURIComponent(nome),'DELETE');
+  refresh();
+}
+
+async function associarTurma(){
+  const mat=document.getElementById('att-aluno').value;
+  const turma=document.getElementById('att-turma').value;
+  if(!mat||!turma){
+    document.getElementById('att-msg').style.color='#ef4444';
+    document.getElementById('att-msg').textContent='Selecione aluno e turma.';
+    return;
+  }
+  const r=await api('/api/alunos/'+encodeURIComponent(mat)+'/turma','POST',{turma});
+  const msg=document.getElementById('att-msg');
+  if(r.ok){
+    msg.style.color='#10b981';
+    msg.textContent='Associação realizada!';
+    refresh();
+  }else{
+    msg.style.color='#ef4444';
+    msg.textContent=r.erro;
+  }
+}
+
 async function addMateria(){
   const nome=document.getElementById('m-nome').value.trim();
   const prof=document.getElementById('m-prof').value.trim();
